# AGENTS.md

## Postman

Resolve the collection from `.cursor/postman.local.json` first (gitignored, per machine), then [POSTMAN.md](POSTMAN.md). MCP connection is `.cursor/mcp.json`; per-PC MCP overrides go in `~/.cursor/mcp.json`. Sync endpoints with `check-endpoint` / `upsert-endpoint` (`.cursor/rules/postman-sync.mdc`).
